# This machine exports NODE_ENV=production, which makes `npm install` omit
# devDependencies — and every build tool here (vite, typescript, eslint,
# vitest) is a devDependency. Forcing dev in means `npm install` does the
# right thing regardless of the ambient NODE_ENV.
include=dev
