# Lane 4, Run 7: layered callouts, worked numbers, the Lego arc, de-Uganda the hook, tic sweep (TEA-948)

Same hard rules as PROMPT.md. Re-read SHARED/REFERENCE-NOTES.md end to end first; the last two notes are this run's content. Apply surgically on top of run 6's state. Teal praised the annotated-equation SVGs and the semantic coloring: that is the bar; keep it.

1. LAYERED DEPTH: adopt Quarto collapsible callouts (collapse="true") as the standard for optional deeper explanation ("prefer it in words? expand this"). The visible default always carries the full message. Convert existing depth layers where natural.
2. WORKED NUMBERS: everywhere an equation is named, add a simple worked numeric example in a collapsible callout, round numbers, one pass through the arithmetic, then one sensitivity line. For the debt dynamics equation: start at 60 percent of GDP, pick round r, g, pb, walk to next year's ratio, then "raise g by one point and the ratio lands at X instead." Same treatment for the growth decomposition.
3. THE LEGO ARC in M1: restructure the narrative as a two-stage reveal: first the base machine, a long-term fiscal projection model built on the debt dynamics equation and the machinery that feeds its three inputs, taught completely; then the warming-scenario block snaps on and the same equation becomes climate-aware (warming lowers g, and weakens pb where spending is rigid). Map v3 supports it: (a) name the actual WEO macro series in the ingredients box (enumerate them from the engine's data_loader; there are not many) and name the controls; (b) add a map variant where the base chain is grayed and the warming block is highlighted, visibly docking on. Vision-QA loop as before.
4. DE-UGANDA THE HOOK: the "deliverable already exists" section currently centers Uganda, which violates the generalization rule. Recast: many ministries publish fiscal-risk documents; at least one published fiscal risk statement already carries a section built with this tool; the worked case examines a real one later. Move the document specifics (pages, scenarios, source line) into M4's worked case. VERIFICATION GATE: every factual claim about the Uganda FRS FY2024/25 (page range, scenario count, the 50 percent ceiling finding, the QCRAFT (2023) source line) must be checked against the actual document on disk in source-materials/; cite precisely what the file shows. If the document is not on disk, download it or cut the specifics and mark the claim unverified in the report. Nothing unverified ships.
5. TIC SWEEP: style-guide-writing-AI.md now carries rule 12, compound assertion-amplification ("X, and Y" restating X bigger). Re-read the rule, then sweep every heading and paragraph in the course for the shape and replace with the stronger construction. Report the count and the before/after list.
DoD: renders pass both profiles; skim discipline holds; report leads with the FRS verification result, then the tic sweep list, then new callouts and map v3 screenshots.
