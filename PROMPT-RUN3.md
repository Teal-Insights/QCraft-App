# Lane 4, Run 3: generalize the course + dual-skin open-source typography (TEA-948)

Run 2 was the skim pass. Run 3 makes two structural changes Teal directed. Same hard rules as PROMPT.md (this clone only, no push, local commits, substance and claims frozen, style guides binding, quarto render must pass). Read SHARED/REFERENCE-NOTES.md again first; it carries the binding decisions.

## Part A: generalize the address, keep Uganda as the worked example
The course must scale beyond Uganda. The reader, named plainly in the preface and M0: someone who has to run, interact with, or otherwise understand this class of fiscal projection tool: ministry-of-finance economists first, and also IFI staff, researchers, and analysts. Changes:
1. Sweep Uganda-specific institutional terminology out of the GENERAL path. "PS" / "Permanent Secretary" becomes a generalized principal ("your director, your minister"); inside M4's worked case, Ugandan terms may stay with a one-line gloss. Course-wide capstone framing becomes "your ministry's fiscal-risk documentation (in the worked example, Uganda's Fiscal Risks Statement)".
2. M0's title changes to a generalized form: build with "Start here: what you will walk into your director's office with", mark the title line DRAFT FOR TEAL, and list it first in MORNING-REPORT.md as an amendment to an approved title.
3. M4 keeps Uganda end to end, reframed explicitly as the worked example on the guide's verification country, with the existing faded handoff ("now do your own country") made prominent. Examples elsewhere may stay Uganda when they are illustrations, with transfer language where cheap.
4. Do not scrub the word Uganda from data, verification claims, or the worked case. This is about who the course addresses, not about hiding the example.

## Part B: dual-skin typography, demonstrably open source
Implement the two-tier font rule (Klim README, 2026-06-16; see SHARED notes):
1. Download and BUNDLE open fonts into the course (fonts/open/): Inter (sans) and IBM Plex Serif (display/headings) and IBM Plex Mono (code), woff2, from their official releases, WITH their OFL license files. Replace the Google Fonts CDN import in _custom.css with self-hosted @font-face for these files, so the course renders identically offline (ministries block CDNs).
2. Define font stacks as CSS variables that PREFER the Klim names and fall back to the open faces: sans "Söhne", "Inter", system-ui; display serif "Tiempos Headline", "IBM Plex Serif", Georgia; mono "Söhne Mono", "IBM Plex Mono", monospace. Apply the display serif to h1/h2 and the sans to body, echoing the brand hierarchy in both skins.
3. Add a Quarto profile named `brand` (_quarto-brand.yml) whose only effect is including an extra stylesheet `_brand-fonts.css` carrying @font-face declarations that point at /fonts/klim/*.woff2 paths. Do NOT add any Klim font file to the repo; those paths exist only on licensed deploys (tealinsights.com). The default render must never reference them; the brand render must degrade gracefully (fall back to the open faces) when the files are absent.
4. Add a short "Typography and reproducibility" section to the repo README and the course colophon: the course is open source (MIT) and builds completely with the bundled open fonts; the Teal Insights house edition uses licensed Klim faces where the license permits; anyone can reproduce everything with the open stack.
5. Verify: `quarto render docs/companion-guide` (default) and `quarto render docs/companion-guide --profile brand` both pass; `git grep -il "soehne\|söhne\|tiempos" -- '*.woff2' '*.otf' '*.ttf'` finds nothing; no Google Fonts CDN references remain.

DoD: both parts done, renders pass, MORNING-REPORT.md lists the M0 title amendment first, then the terminology sweep summary (terms changed, counts), then the font implementation with the reproducibility claim verified.

## Amendments (Teal, 8/26 evening; these override anything above that conflicts)
1. MULTI-COUNTRY EXEMPLIFICATION, not Uganda-as-the-example. Do not frame the course as anyone's course, and do not default examples to Uganda. Each concept draws its example from whichever country makes the mechanism vivid: demography wants a shrinking-workforce economy contrasted with a fast-growing one; productivity convergence wants a catch-up economy; the climate channel wants a highly exposed economy. Choose from the tool's country set; Ethiopia and Zambia are good candidates to include among examples. The M4 full walkthrough may run on Uganda (it is the verification country) but presents as "a worked case" among countries the reader can run, and the faded variants (completion and independent problems) run on OTHER countries. Test: a reader in Addis or Lusaka never feels this is a Uganda document.
2. PRINCIPAL FRAMING: never name a specific office as the target of the capstone (no PS, no minister's office, no director's office). The deliverable is defended to the senior officials who sign off. Build M0's title as "Start here: the analysis you will defend" and stage two or three alternatives in MORNING-REPORT.md, all flagged DRAFT FOR TEAL as amendments to an approved title.
3. THE INTUITION MAP: kill the "seven modules" node. It names a count where every other node names a function. The canonical teaching chain is: country data + your assumptions are manufactured into THE THREE NUMBERS the debt equation needs: growth g (built from demography, productivity, inflation), the interest rate r, and the primary balance pb (revenue, spending, rigidity, the fiscal rule); climate scenarios shift g (and pb when spending is rigid); the equation turns the three into debt paths. Rebuild the M1 concept map on that chain. "Seven modules" survives only as a one-line architecture aside linking to the repo. Amend M1's title accordingly: build with "One equation decides the debt path; the rest of the tool builds its three inputs" and stage alternatives, DRAFT FOR TEAL, flagged as an amendment to an approved title.
